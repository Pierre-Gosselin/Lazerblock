{% extends 'layout.html.twig' %}

{% block title %}Présentation{% endblock %}

{% block content %}
    <div class="container">
        <div class="row mt-4 mb-4">
            <div class="col">
                <div class="card ombre" style="background-color: rgba(0,0,0,0.5);color:white;">
                    <div class="card-body">
                        <h3 class="card-title text-center font">Contactez nous</h3>
                    </div>
                </div>
            </div>
        </div>
        <div class="row mt-4">
            <div class="col">
                <div class="card ombre px-3 pt-3" style="background-color: rgba(0,0,0,0.5);color:white;">
                    {{ form_start(form) }}
                        <div class="form-group">
                            {{ form_row(form.email, {'attr': {'value': userEmail }} ) }}
                        </div>
                        <div class="form-group">
                            {{ form_row(form.sujet) }}
                        </div>
                        <div class="form-group">
                            {{ form_row(form.message) }}
                        </div>
                        <div class="form-group text-center">
                            <button class="btn btn-outline-light btn-lg">Envoyer</button>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}