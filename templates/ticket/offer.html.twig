{% extends 'layout.html.twig' %}

{% block title %}Offrir un ticket{% endblock %}

{% block content %}
<div class="container">
    <div class="row">
        <div class="col-6 offset-3 card p-3 mt-5" style="background-color: rgba(0, 0, 0, 0.5);">
            <form method="post">
                <h3 class="h3 mb-3 font-weight-normal">Offrir un ticket à un amis.</h3>

                <label for="defaultRegisterFormEmail">Saisissez l'adresse email de votre amis</label>
                <input type="email" id="defaultRegisterFormEmail" class="form-control mb-3" placeholder="E-mail" name="email" required>
                
                <button class="btn btn-outline-light" type="submit">Offrir</button>
                <a href="{{ path('ticket_show') }}" class="btn btn-outline-light">Annuler</a>
            </form>
        </div>
    </div>
</div>
{% endblock %}
